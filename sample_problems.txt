// Problem 1: Check if a number is positive
1
Arrange the code to define a function that returns True if n is positive, False otherwise.
f|0|def is_positive(n):
f|1|    if n > 0:
f|2|        return True
f|3|    else:
f|4|        return False
t|1|    if n >= 0:

---

// Problem 2: Sum a list using a for loop
2
Arrange the code to define a function that returns the sum of all elements in a list.
f|0|def sum_list(lst):
f|1|    total = 0
f|2|    for x in lst:
f|3|        total += x
f|4|    return total
t|3|        total -= x

---

// Problem 3: Absolute value
3
Arrange the code to define a function that returns the absolute value of n.
f|0|def absolute(n):
f|1|    if n < 0:
f|2|        return -n
f|3|    return n
t|1|    if n > 0:

---

// Problem 4: Print even numbers from 0 to 10
4
Arrange the code to print all even numbers from 0 to 10 inclusive.
f|0|for i in range(11):
f|1|    if i % 2 == 0:
f|2|        print(i)
t|1|    if i % 2 == 1:

---

// Problem 5: Find the maximum value in a list
5
Arrange the code to define a function that returns the largest value in a list.
f|0|def find_max(lst):
f|1|    max_val = lst[0]
f|2|    for x in lst:
f|3|        if x > max_val:
f|4|            max_val = x
f|5|    return max_val
t|3|        if x < max_val:

---

// Problem 6: Letter grade from score
6
Arrange the code to define a function that returns a letter grade for a given numeric score.
f|0|def get_grade(score):
f|1|    if score >= 90:
f|2|        return "A"
f|3|    elif score >= 80:
f|4|        return "B"
f|5|    elif score >= 70:
f|6|        return "C"
f|7|    else:
f|8|        return "F"
t|3|    elif score >= 60: